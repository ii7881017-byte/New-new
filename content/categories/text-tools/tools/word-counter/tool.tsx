"use client";import{useMemo,useState}from"react";export default function Tool(){const[t,setT]=useState("");const w=useMemo(()=>t.trim()?t.trim().split(/\s+/).length:0,[t]);return <div className="tool"><textarea rows={10} value={t} onChange={e=>setT(e.target.value)} placeholder="Type or paste your text..."/><div className="badges"><div className="badge"><b>{w}</b>Words</div><div className="badge"><b>{t.length}</b>Characters</div></div></div>}